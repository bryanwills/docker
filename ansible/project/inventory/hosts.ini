# Personal VPS inventory.
# The VPS can manage itself (localhost) plus any extra hosts you add.

[local]
localhost ansible_connection=local

[vps]
# Add remote hosts like:
# myhost ansible_host=10.0.0.5 ansible_user=bryan
